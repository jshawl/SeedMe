<%= form_for [@receiver, @message], html: {class: "new-message"} do |f| %>
  <%= f.label :subject %>
  <%= f.text_field :subject, size: 40 %>

  <%= f.label :body %>
  <%= f.text_area :body, size: "50x20"%>

  <%= f.submit "Send Message" %>
<% end %>
